services:
  - type: web
    name: noyyal-api
    env: node
    region: oregon
    plan: free
    rootDir: services/api-node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
